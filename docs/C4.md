# C4 do Prumo

## Nível 1 - contexto

```mermaid
C4Context
  title Prumo - contexto do sistema
  Person(master, "Master/administrador", "Gerencia empresas, usuários, pagamentos e operação")
  Person(operador, "Operador fiscal", "Cria runs, acompanha logs e baixa artefatos")
  System(prumo, "Prumo", "Orquestra ISS Fortaleza e Portal Nacional")
  System_Ext(iss, "ISS Fortaleza", "Portal municipal")
  System_Ext(nfse, "Portal Nacional NFS-e", "Consulta e download de XML/DANFSe")
  System_Ext(ai, "Resolvedor hCaptcha", "Google Modo IA: duas contas Modal e ThinkPad de contingência")
  Rel(master, prumo, "Administra via HTTPS")
  Rel(operador, prumo, "Usa via HTTPS")
  Rel(prumo, iss, "Automatiza navegador")
  Rel(prumo, nfse, "Sessão mTLS e requests")
  Rel(prumo, ai, "Solicita tokens hCaptcha")
```

## Nível 2 - contêineres

```mermaid
C4Container
  title Prumo - contêineres
  Person(operador, "Operador fiscal")
  Container_Boundary(prumo, "Prumo") {
    Container(ui, "Frontend estático", "HTML/CSS/JS no Worker; Netlify como publicação complementar", "Login, master, admin, ISS e Portal Nacional")
    Container(edge, "Worker de borda", "Cloudflare Worker + D1", "Autenticação, autorização e proxy interno")
    ContainerDb(d1, "D1", "Cloudflare D1", "Empresas, usuários, sessões, billing e auditoria")
    Container(api, "API Prumo", "FastAPI/Python", "Filas, runs, certificados e artefatos")
    ContainerDb(data, "Dados persistentes", "SQLite + filesystem", "Estado por empresa/colaborador")
    Container(browser, "Browserless ISS", "Modal/Chromium", "Fluxos do ISS Fortaleza")
    Container(solver, "Pool de solvers Portal", "Duas contas Modal + Python + Chromium + Google Modo IA", "Failover de quota por endpoint; até quatro containers por conta")
    Container(solverLocal, "Solver Portal residencial", "ThinkPad + Python + Chromium + Google Modo IA", "Fallback local e estado persistente")
  }
  System_Ext(iss, "ISS Fortaleza")
  System_Ext(nfse, "Portal Nacional NFS-e")
  Rel(operador, ui, "HTTPS")
  Rel(ui, edge, "Fetch autenticado")
  Rel(edge, d1, "Lê e grava")
  Rel(edge, api, "Cloudflare Tunnel + segredo interno")
  Rel(api, data, "Lê e grava")
  Rel(api, browser, "Jobs Browserless")
  Rel(browser, iss, "HTTPS direto; proxy opcional")
  Rel(api, nfse, "mTLS/HTTPS direto do ThinkPad; sessão, índice e downloads")
  Rel(api, solver, "HTTPS /solve; rota primária")
  Rel(api, solverLocal, "HTTP local /solve; somente após as contas Modal")
```

## Nível 3 - componentes da API

```mermaid
C4Component
  title API Prumo - componentes principais
  Container_Boundary(api, "FastAPI") {
    Component(auth, "WorkerContext", "domain.py", "Isola empresa e colaborador")
    Component(queue, "Fila global justa", "run_queue.py", "Round-robin por membro e retry adaptativo")
    Component(issFlows, "Fluxos ISS", "flow_*.py", "Login, notas, DAM e escrituração")
    Component(portalApi, "Portal API", "portal_nacional.py", "Certificados, runs e downloads")
    Component(portalFlow, "Portal automation", "portal_nacional_automation.py", "Sessão, índice, captcha e arquivos")
    Component(checkpoint, "Checkpoint", "JSON/SQLite", "Retomada idempotente e logs")
  }
  Rel(auth, queue, "Autoriza e delimita")
  Rel(queue, issFlows, "Executa")
  Rel(portalApi, portalFlow, "Subprocesso controlado")
  Rel(issFlows, checkpoint, "Atualiza")
  Rel(portalFlow, checkpoint, "Atualiza")
```

## Decisões arquiteturais

- ISS usa Modal direto por padrão porque o A/B real mostrou menor latência e menos falhas DNS; a proxy continua como fallback.
- O ThinkPad autentica no Portal Nacional diretamente em `certificado.nfse.gov.br`, indexa as notas e baixa os arquivos. Apenas a resolução visual do hCaptcha vai para o Modal. Não há proxy no caminho normal do Portal.
- Se o Modal principal atingir quota, retornar circuito aberto ou ficar indisponível, a resolução visual segue para uma segunda conta Modal. `127.0.0.1:8876` é o último recurso. O PFX, a senha e a sessão do Portal nunca são enviados ao Modal.
- O único provedor visual é o Google Modo IA. Não há Florence nem Cohere e não há resolvedores separados para 9 tiles e outros formatos: um contrato visual único escolhe `selecionar_regioes` ou `clicar_ponto`; somente o adaptador mecânico de clique muda conforme o DOM. O código fica em `solver/google_ai_mode`; estado anônimo fica em Volume privado no Modal e no ThinkPad.
- Cada conta Modal pode escalar até quatro containers com um navegador por container. O Portal usa cooldown independente por endpoint e backoff crescente quando o serviço externo está degradado, sem consumir tentativas repetindo imediatamente um erro global.
- Login, master e admin são servidos pelo mesmo Worker da API com `no-store`, evitando divergência de versão quando o Netlify está sem créditos. O GitHub continua sendo a fonte; o auto-deploy Netlify pode voltar sem mudar a arquitetura.
- Artefatos visuais ficam por sete dias, mas HTML/JSON/texto são gzipados e PNG vira WebP lossless após 15 minutos. XML/PDF das empresas não entram nessa limpeza. Logs Docker têm rotação de 3 arquivos de 10 MiB.
- A UI do ISS consulta apenas 20 mil caracteres recentes; o servidor mantém cache incremental por CNPJ/fluxo para mostrar novos logs sem reler o arquivo inteiro.
- Certificados são validados antes de entrar na run; falha de descriptografia nunca vira senha vazia silenciosa.
