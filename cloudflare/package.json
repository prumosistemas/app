{
  "name": "prumo-cloudflare-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node --check worker.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
